apiVersion: v1
kind: Service
metadata:
  name: vllm
  namespace: openclaw-llms
  labels:
    app: vllm
spec:
  type: ClusterIP
  selector:
    app: vllm
    component: inference
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
